# `src/scripts/__init__.py`

## What this file does
This file is intentionally empty. It marks `src/scripts` as a Python package.

## Why it exists
It allows Python to import files inside the `scripts` folder as modules.

## In plain words
This file does not contain training logic. It simply helps package imports work correctly.

