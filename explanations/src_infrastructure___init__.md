# `src/infrastructure/__init__.py`

## What this file does
This file is intentionally empty. It marks `src/infrastructure` as a Python package.

## Why it exists
It allows imports such as:

```python
from infrastructure import utils
from infrastructure import pytorch_util as ptu
```

## In plain words
This file has no direct logic. It just makes the folder importable as a package.

