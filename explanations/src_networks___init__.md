# `src/networks/__init__.py`

## What this file does
This file is intentionally empty. It marks `src/networks` as a Python package.

## Why it exists
It allows imports like:

```python
from networks.policies import MLPPolicyPG
from networks.critics import ValueCritic
```

## In plain words
This file is just a package marker. It does not define any behavior by itself.

